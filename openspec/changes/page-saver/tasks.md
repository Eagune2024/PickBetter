## 1. 项目初始化和基础架构

- [x] 1.1 创建 `source/utils/pageSaver/` 目录结构
  - **验收标准**: 目录包含 index.ts, dom-processor.ts, css-processor.ts, image-processor.ts, resource-fetcher.ts, html-generator.ts, types.ts
  - **依赖**: 无

- [x] 1.2 定义 TypeScript 类型定义(`types.ts`)
  - **验收标准**: 定义 PageSaverConfig, ProcessResult, ResourceCache 等核心类型
  - **依赖**: 1.1

- [x] 1.3 实现资源获取器(`resource-fetcher.ts`)
  - **验收标准**: 实现 fetchResource, ResourceCache 类,支持 CORS 错误处理和缓存机制
  - **依赖**: 1.2

## 2. DOM 处理器实现

- [x] 2.1 实现 DOM 克隆功能(`dom-processor.ts`)
  - **验收标准**: 实现 cloneDocument 方法,能够创建完整的 DOM 副本
  - **测试**: 在测试页面验证克隆后的 DOM 独立于原始 DOM
  - **依赖**: 1.2

- [x] 2.2 实现 Canvas 元素转换
  - **验收标准**: detectCanvases 和 convertCanvases 方法,将 Canvas 转换为 Base64 图片
  - **测试**: 在包含 Canvas 的页面验证转换功能
  - **依赖**: 2.1

- [x] 2.3 实现 Shadow DOM 处理(可选,高级功能)
  - **验收标准**: processShadowDOM 方法,序列化 Shadow Root 内容
  - **测试**: 在包含 Shadow DOM 的页面验证功能
  - **依赖**: 2.1

- [x] 2.4 实现 iframe 处理(可选,高级功能)
  - **验收标准**: processFrames 方法,处理同源 iframe,保留跨源 iframe 引用
  - **测试**: 在包含 iframe 的页面验证功能
  - **依赖**: 2.1

## 3. CSS 处理器实现

- [x] 3.1 实现内部样式提取(`css-processor.ts`)
  - **验收标准**: extractInlineStyles 方法,提取所有 `<style>` 标签内容
  - **测试**: 在包含多个 `<style>` 标签的页面验证提取功能
  - **依赖**: 1.3

- [x] 3.2 实现外部 CSS 文件获取
  - **验收标准**: fetchExternalCSS 方法,获取所有 `<link rel="stylesheet">` 文件
  - **测试**: 在包含外部 CSS 的页面验证获取功能
  - **依赖**: 3.1

- [x] 3.3 实现 CSS URL 转换
  - **验收标准**: processCSSURLs 方法,将相对 URL 转换为绝对 URL
  - **测试**: 验证 `url(../images/bg.png)` 被正确转换为绝对 URL
  - **依赖**: 3.2

- [x] 3.4 实现 @import 规则处理
  - **验收标准**: processImports 方法,递归获取导入的 CSS 文件
  - **测试**: 在包含 @import 的 CSS 文件验证功能
  - **依赖**: 3.3

- [ ] 3.5 实现 CSS 变量解析
  - **验收标准**: resolveCSSVariables 方法,保留并正确处理 `var(--name)` 引用
  - **测试**: 在使用 CSS 变量的页面验证功能
  - **依赖**: 3.4

- [x] 3.6 实现 CSS 合并和内联
  - **验收标准**: inlineCSS 方法,将所有 CSS 合并为单个 `<style>` 标签并插入文档
  - **测试**: 验证最终文档包含合并后的 `<style>` 标签
  - **依赖**: 3.5

## 4. 图片处理器实现

- [x] 4.1 实现图片转 Base64 功能(`image-processor.ts`)
  - **验收标准**: convertToBase64 方法,使用 fetch 和 FileReader 转换图片
  - **测试**: 验证图片 URL 被正确转换为 data URI 格式
  - **依赖**: 1.3

- [x] 4.2 实现 `<img>` 标签处理
  - **验收标准**: processImgTags 方法,处理所有 `<img>` 标签的 src 属性
  - **测试**: 在包含多张图片的页面验证功能
  - **依赖**: 4.1

- [x] 4.3 实现懒加载图片处理
  - **验收标准**: 优先使用 `data-src` 属性,回退到 `src` 属性
  - **测试**: 在使用懒加载的页面验证功能
  - **依赖**: 4.2

- [x] 4.4 实现 CSS 背景图处理
  - **验收标准**: processBackgroundImages 方法,提取并转换 `background-image` 中的图片
  - **测试**: 在使用背景图的元素上验证功能
  - **依赖**: 4.1

- [x] 4.5 实现 srcset 属性处理
  - **验收标准**: processSrcSet 方法,处理 srcset 中的所有图片 URL
  - **测试**: 在包含 srcset 的 `<img>` 标签验证功能
  - **依赖**: 4.2

- [x] 4.6 实现 `<picture>` 元素处理
  - **验收标准**: processPictureElements 方法,处理 `<source>` 和 `<img>` 的所有图片源
  - **测试**: 在包含 `<picture>` 的页面验证功能
  - **依赖**: 4.5

- [x] 4.7 实现资源去重机制
  - **验收标准**: 资源缓存使用 Base64 内容的 hash 作为 key,相同资源只转换一次
  - **测试**: 在多个元素引用相同图片的页面验证去重功能
  - **依赖**: 4.6

## 5. HTML 生成器实现

- [x] 5.1 实现元数据添加功能(`html-generator.ts`)
  - **验收标准**: addMetadata 方法,添加保存来源 URL 和时间戳
  - **测试**: 验证生成的 HTML 包含正确的 meta 标签
  - **依赖**: 无

- [x] 5.2 实现 HTML 序列化功能
  - **验收标准**: generate 方法,生成包含 `<!DOCTYPE html>` 的完整 HTML 字符串
  - **测试**: 验证生成的 HTML 字符串格式正确
  - **依赖**: 5.1

- [x] 5.3 实现文件下载功能
  - **验收标准**: download 方法,创建 Blob 并触发浏览器下载
  - **测试**: 验证点击后正确下载 HTML 文件
  - **依赖**: 5.2

- [x] 5.4 实现文件命名功能
  - **验收标准**: generateFilename 方法,使用"页面标题-时间戳.html"格式
  - **测试**: 验证文件名包含页面标题且唯一
  - **依赖**: 5.3

## 6. PageSaver 主入口实现

- [x] 6.1 实现 PageSaver 主类(`index.ts`)
  - **验收标准**: PageSaver 类,协调所有处理器,暴露 savePage 方法
  - **测试**: 验证 savePage 方法能够正确调用各个处理器
  - **依赖**: 2.6, 3.6, 4.7, 5.4

- [x] 6.2 实现进度回调机制
  - **验收标准**: 支持传入 onProgress 回调,实时更新处理阶段和百分比
  - **测试**: 验证在保存过程中正确调用进度回调
  - **依赖**: 6.1

- [x] 6.3 实现错误处理和日志记录
  - **验收标准**: 捕获所有处理错误,记录到 console,不中断流程
  - **测试**: 模拟各种错误场景验证处理逻辑
  - **依赖**: 6.2

## 7. UI 集成

- [x] 7.1 在 AI 对话框中添加"保存页面"按钮(`aiDialogManager.ts`)
  - **验收标准**: 修改 showSuccess 方法,在成功消息后添加保存按钮
  - **测试**: AI 修改成功后显示保存按钮
  - **依赖**: 6.3

- [x] 7.2 实现按钮点击事件处理
  - **验收标准**: 点击按钮后调用 PageSaver.savePage,显示进度提示
  - **测试**: 点击按钮后触发保存流程
  - **依赖**: 7.1

- [x] 7.3 实现按钮状态管理
  - **验收标准**: 保存时按钮禁用并显示"保存中...",完成后显示"已保存"或"保存失败"
  - **测试**: 验证按钮状态在保存过程中正确变化
  - **依赖**: 7.2

- [x] 7.4 实现进度指示器UI
  - **验收标准**: 在对话框中显示进度条和文本提示
  - **测试**: 验证进度提示正确显示当前处理阶段
  - **依赖**: 7.3

- [x] 7.5 添加保存成功/失败反馈
  - **验收标准**: 保存完成后显示成功或失败消息,失败时显示错误原因
  - **测试**: 验证各种保存结果显示正确
  - **依赖**: 7.4

## 8. 测试和优化

- [ ] 8.1 在真实网页上进行端到端测试
  - **验收标准**: 在不同类型的网页(新闻页、博客页、电商页)测试保存功能
  - **测试**: 验证保存的 HTML 文件可以在浏览器中正常打开和显示
  - **依赖**: 7.5

- [ ] 8.2 性能测试和优化
  - **验收标准**: 在包含大量图片的页面测试保存时间,必要时实现分批处理
  - **测试**: 大页面(> 50 张图片)保存时间不超过 10 秒
  - **依赖**: 8.1

- [ ] 8.3 CORS 限制场景测试
  - **验收标准**: 在包含 CORS 限制资源的页面测试,验证优雅降级
  - **测试**: 验证无法获取的资源被跳过,不中断保存流程
  - **依赖**: 8.1

- [ ] 8.4 跨浏览器兼容性测试
  - **验收标准**: 在 Chrome 和 Firefox 上测试所有功能
  - **测试**: 验证两个浏览器上的行为一致
  - **依赖**: 8.3

- [ ] 8.5 边缘情况处理测试
  - **验收标准**: 测试空页面、纯文本页面、包含特殊字符的页面等边缘情况
  - **测试**: 验证所有边缘情况都能正确处理
  - **依赖**: 8.4

## 9. 文档和交付

- [x] 9.1 添加代码注释和文档
  - **验收标准**: 所有公共方法包含 JSDoc 注释,复杂逻辑包含行内注释
  - **依赖**: 8.5

- [ ] 9.2 更新用户文档(如需要)
  - **验收标准**: 在 README 或用户指南中说明如何使用保存功能
  - **依赖**: 9.1

- [x] 9.3 代码审查和修复
  - **验收标准**: 通过 ESLint 检查,修复所有 lint 错误和警告
  - **测试**: 运行 `npm run lint` 无错误
  - **依赖**: 9.2
